<style>
  body { padding-top: 58px; font-family: 'Segoe UI', sans-serif; }
  .admin-topbar {
    position: fixed; top: 0; left: 0; right: 0; z-index: 9999;
    background: linear-gradient(135deg, #1a1a2e 0%, #0f3460 100%);
    padding: 11px 24px;
    display: flex; justify-content: space-between; align-items: center;
    box-shadow: 0 2px 20px rgba(0,0,0,0.5);
    border-bottom: 1px solid rgba(233,69,96,0.2);
  }
  .admin-topbar-brand { color: #e94560; font-weight: 800; font-size: 16px; text-decoration: none; display: flex; align-items: center; gap: 8px; }
  .admin-topbar-brand:hover { color: #ff7e92; }
  .admin-badge { background: rgba(233,69,96,0.2); color: #f87171; font-size: 10px; font-weight: 700; padding: 2px 8px; border-radius: 5px; text-transform: uppercase; letter-spacing: 1px; }
  .admin-topbar-links { display: flex; gap: 6px; align-items: center; }
  .admin-topbar-links a { color: rgba(255,255,255,0.72); text-decoration: none; font-size: 12.5px; font-weight: 500; padding: 6px 12px; border-radius: 7px; transition: all 0.2s; }
  .admin-topbar-links a:hover { color: #fff; background: rgba(255,255,255,0.08); }
  .admin-topbar-links a.logout-btn { background: #e94560; color: #fff; font-weight: 600; }
  .admin-topbar-links a.logout-btn:hover { background: #c4294a; }
</style>

<nav class="admin-topbar">
  <a href="/admin" class="admin-topbar-brand">
    ⚡ ProjectHub
    <span class="admin-badge">Admin</span>
  </a>
  <div class="admin-topbar-links">
    <a href="/admin">Dashboard</a>
    <a href="/admin/orders">Orders</a>
    <a href="/admin/contact-request-list">Contacts</a>
    <a href="/" target="_blank">View Site</a>
    <a href="/admin/logout" class="logout-btn">Logout</a>
  </div>
</nav>